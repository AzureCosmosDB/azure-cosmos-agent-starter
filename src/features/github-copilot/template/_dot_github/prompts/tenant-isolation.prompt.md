Trace every identity source and query boundary. Reject model-provided tenant or user IDs and require
authenticated RequestContext values in reads, writes, deletion, vector retrieval, and action approval.
