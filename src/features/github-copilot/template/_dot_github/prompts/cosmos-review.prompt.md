Review Cosmos DB code for singleton client use, parameterized and bounded queries, partition-key scoping,
continuation-token pagination, RU diagnostics, retry behavior, and production Managed Identity.
